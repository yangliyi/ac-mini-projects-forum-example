<% if @post.errors.any? %>
  <ul>
     <% @post.errors.full_messages.each do |msg| %>
        <li style="color:red"><%= msg %></li>
      <% end %>
  </ul>
<% end %>

<%= form_for @post do |f| %>

<%= render "form", :f => f %>

<% end %>

<%= link_to 'Back to index page', posts_path %>